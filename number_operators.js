//operadores aritmeticos +, -, *, / e %

2 + 2;
8 - 3;
4 * 5;
9 / 3;
7 % 2;

//Operadores de atribução  +=, -=, *=, /= e %=

let result = 10;
result += 2;
result -= 5;
result *= 8;
result /= 2;
result %= 6;

//operadores de incremento e decrement ++ e --

let res = 10;

res++;  //post increment
++res; //pre increment

res--;
--res;

//operators binary 
// | Or
// & AND
// ^ SHORT or Inclusived
// ~ NOT
// <<  deslocate left
// >>  deslocate right
// >>> deslocate right with change sinal 

4 | 3;
(4).toString(2)
(4).toString(2).padStart(32,0)
3 & 1;
5 ^ 2;
~2;
4 << 2;
128 >> 1;
-2 >>> 1;
(-2 >>> 0).toString(2).padStart(32,0)
